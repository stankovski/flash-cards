<UserControl
    x:Class="FlashCards.CardSideView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlashCards"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="CardSideViewControl"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Width="150" Height="200">
        <!--<Image Grid.RowSpan="2" Stretch="Fill" Source="Assets/paper.jpg" VerticalAlignment="Center" HorizontalAlignment="Center"/>-->
        <Grid x:Name="TextOnlyCard" Background="White">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" TextWrapping="WrapWholeWords" Margin="10"
                        Text="{Binding ElementName=CardSideViewControl, Path=Text}"/>
        </Grid>
    </Grid>
</UserControl>
